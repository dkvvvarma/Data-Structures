#include<stdio.h>
int main ()
{
  int key=72,i,n=7;
  int arr[7] = {0,18,99,80,72,65,85};
  for(i=0;i<n;i++)
    {
      if(arr[i]==key)
      {
        printf("%d ",i+1);
        break;
      } 
    }
  if(i==n)
  {
    printf("Retry");
  }
  return 0;
}